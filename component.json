{
  "name": "model-memoize",
  "repo": "segmentio/model-memoize",
  "description": "Memoizes the model objects returned from the server to reduce outbound requests and speed up initial page loading.",
  "keywords": ["model", "cache", "memoize"],
  "license": "MIT",
  "version": "0.0.1",
  "dependencies": {
    "component/each": "*",
    "component/type": "*"
  },
  "scripts": [
    "index.js"
  ],
  "development": {
    "component/assert": "*",
    "component/model": "*"
  }
}